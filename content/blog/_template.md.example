---
# 必填 — 文章标题
title: "标题写在这里"

# 必填 — 发布日期,格式 YYYY-MM-DD。
# 建议加引号:不加引号的 2026-05-22 会被 YAML 解析成 Date 对象
# (lib/content-loader.ts 的 normalizeDate 会兜底规整,但加引号最稳妥)。
date: "2026-01-01"

# 可选 — 列表页/搜索结果里显示的摘要;留空则文章卡片不显示摘要
description: ""

# 可选 — 标签数组;没有就留 []
tags: [示例, 标签]

# 可选 — 草稿:写上 draft: true 后仅 npm run dev 可见,build 不会收录;发布前删掉这行
# draft: true
---

## 第一个小标题

正文从这里开始。支持 `行内代码`、带语言标记的代码围栏、GFM 表格、$行内公式$ 与 $$块级公式$$。
中文排版注意:**粗体**和~~删除线~~紧贴中文也能正常渲染(依赖 CJK 友好的 remark 插件)。

<!--
  英文版:同 slug 放在 content/blog/en/<slug>.md,双语齐备后详情页会出现「中 / EN」切换徽章。
  更多写法见 DEVELOPMENT.md §2。
-->
